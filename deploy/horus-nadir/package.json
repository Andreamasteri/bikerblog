{
  "name": "horus-nadir-service",
  "version": "1.0.0",
  "private": true,
  "description": "Nadir — servizio di ricerca semantica (embedding all-minilm via Ollama) per Horus e Bowie, in esecuzione su TC.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
